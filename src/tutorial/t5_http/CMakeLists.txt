set(ProjectName "tutorial_http")
file(GLOB soruces *.cc)
file(GLOB headers *.h)
add_library(${ProjectName} SHARED ${soruces} ${headers})
LinkSquick(${ProjectName})
SquickOut(${ProjectName} "tutorial/http" ${SQUICK_BUILD_PLUGIN_TUTORIAL_PATH} "http")