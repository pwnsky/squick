set(ProjectName "gameplay_client")
file(GLOB soruces *.cc)
file(GLOB headers *.h)
add_library(${ProjectName} SHARED ${soruces} ${headers})
LinkSquick(${ProjectName})
SquickOut(${ProjectName} "server/gameplay" ${SQUICK_BUILD_PLUGIN_SERVER_PATH}/gameplay "client")