set(ProjectName "ds_mgr_logic")
file(GLOB soruces *.cc)
file(GLOB headers *.h)
add_library(${ProjectName} SHARED ${soruces} ${headers})
LinkStruct(${ProjectName})
SquickOut(${ProjectName} "node/ds_mgr" ${SQUICK_BUILD_PLUGIN_SERVER_PATH}/ds_mgr "logic")