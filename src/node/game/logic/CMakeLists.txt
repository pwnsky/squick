set(ProjectName "game_logic")
file(GLOB soruces *.cc)
file(GLOB headers *.h)
add_library(${ProjectName} SHARED ${soruces} ${headers})
LinkStruct(${ProjectName})
SquickOut(${ProjectName} "node/game" ${SQUICK_BUILD_PLUGIN_SERVER_PATH}/game "logic")